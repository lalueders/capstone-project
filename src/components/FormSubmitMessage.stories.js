import FormSubmitMessage from './FormSubmitMessage';
export default {
  title: 'FormSubmitMessage',
  component: FormSubmitMessage,
};

const Template = args => <FormSubmitMessage {...args} />;

export const Default = Template.bind({});
Default.args = {};
